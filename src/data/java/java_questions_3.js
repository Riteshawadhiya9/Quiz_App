// Java Questions — Batch 3: Exception Handling, Generics, Multithreading, Memory, APIs (40 questions)
export const JAVA_Q3 = [
  // ═══ EXCEPTION HANDLING (8) ═══
  { id:"java71", diff:"easy", topic:"Exceptions", q:"What is the difference between checked and unchecked exceptions?", opts:["No difference","Checked: must be declared/handled at compile time (IOException). Unchecked: RuntimeException subclasses, no compile-time enforcement (NullPointerException, ArrayIndexOutOfBounds)","Checked crash JVM","Unchecked must be caught"], ans:1, exp:"Checked: compiler forces try-catch or throws declaration. Unchecked: programming errors (bugs), not enforced. Error (OutOfMemoryError) is also unchecked but indicates JVM-level problems." },
  { id:"java72", diff:"easy", topic:"Exceptions", q:"What does the 'finally' block guarantee?", opts:["Runs only on exception","Always executes regardless of exception (except System.exit() or JVM crash) — used for cleanup","Runs before try","Replaces catch"], ans:1, exp:"finally always runs: after try (no exception), after catch (exception handled), even if return is in try/catch. Ideal for closing resources. Java 7 try-with-resources is preferred for AutoCloseable." },
  { id:"java73", diff:"medium", topic:"Exceptions", q:"What is try-with-resources?", opts:["Try with multiple catch blocks","try(Resource r = new Resource()) { } — automatically closes AutoCloseable resources when block exits, even on exception","Try without catch","Try with finally only"], ans:1, exp:"Java 7+: resources declared in try() are auto-closed in reverse order. Replaces verbose try-finally. Resource must implement AutoCloseable. Suppressed exceptions are tracked." },
  { id:"java74", diff:"medium", topic:"Exceptions", q:"Can we have try without catch?", opts:["No, always need catch","Yes — try-finally (cleanup without handling) or try-with-resources (auto-close) are valid","Only in Java 11+","Only with checked exceptions"], ans:1, exp:"try-finally: valid, used when you want cleanup but want exception to propagate. try-with-resources: valid without catch/finally. Must have at least one of: catch, finally, or resources." },
  { id:"java75", diff:"hard", topic:"Exceptions", q:"What is the output?\ntry { return 1; } finally { return 2; }", opts:["1","2 — finally's return overrides try's return","Compile error","Runtime error"], ans:1, exp:"finally runs before the method actually returns. If finally has a return, it overwrites the return value from try/catch. This is bad practice — never return from finally." },
  { id:"java76", diff:"scenario", topic:"Exceptions", q:"Scenario: Which exception hierarchy is correct?", opts:["Exception extends RuntimeException","Throwable ← Exception ← RuntimeException. Throwable ← Error. Both Exception and Error extend Throwable","Error extends Exception","RuntimeException extends Error"], ans:1, exp:"Throwable is the root. Error: JVM problems (OutOfMemory, StackOverflow). Exception: recoverable. RuntimeException extends Exception: unchecked. Everything else under Exception is checked." },
  { id:"java77", diff:"scenario", topic:"Exceptions", q:"Scenario: You catch Exception. Will it also catch RuntimeException?", opts:["No","Yes — RuntimeException extends Exception, so catch(Exception e) catches both checked and unchecked exceptions","Only if specified","Depends on JVM"], ans:1, exp:"catch(Exception e) catches ALL exceptions (checked + unchecked) because RuntimeException IS-A Exception. catch(Throwable t) would also catch Errors. Be specific: catch the most specific exception first." },
  { id:"java78", diff:"scenario", topic:"Exceptions", q:"Scenario: Custom exception — should it extend Exception or RuntimeException?", opts:["Always Exception","If caller should be forced to handle it → checked (extends Exception). If it's a programming error → unchecked (extends RuntimeException)","Always RuntimeException","Extend Error"], ans:1, exp:"Checked: anticipated conditions (FileNotFoundException). Unchecked: programmer mistakes (IllegalArgumentException). Modern frameworks (Spring) favor unchecked exceptions to reduce boilerplate." },

  // ═══ GENERICS (8) ═══
  { id:"java79", diff:"easy", topic:"Generics", q:"What is the purpose of Generics in Java?", opts:["Make code faster","Provide compile-time type safety — catch type errors at compile time instead of ClassCastException at runtime. Eliminate explicit casting","Dynamic typing","Runtime type checking"], ans:1, exp:"Without generics: List list = new ArrayList(); list.add(\"hello\"); int x = (Integer)list.get(0) → ClassCastException at runtime. With: List<String> → compile error on wrong type." },
  { id:"java80", diff:"medium", topic:"Generics", q:"What is type erasure?", opts:["Deleting unused types","At compile time, generic type information is removed and replaced with Object (or bound type). Generics exist only at compile time, not runtime","Removing deprecated types","Clearing type cache"], ans:1, exp:"List<String> and List<Integer> become just List at runtime. Cannot do: new T(), T.class, instanceof T. This maintains backward compatibility with pre-generics code." },
  { id:"java81", diff:"medium", topic:"Generics", q:"What is the difference between <? extends T> and <? super T>?", opts:["Same thing","<? extends T>: upper bound, read-only (producer). <? super T>: lower bound, write-only (consumer). PECS: Producer Extends, Consumer Super","extends is for classes; super is for interfaces","extends is write; super is read"], ans:1, exp:"PECS: List<? extends Number> → can read as Number, can't add. List<? super Integer> → can add Integer, read as Object. Collections.copy(List<? super T> dest, List<? extends T> src) uses both." },
  { id:"java82", diff:"medium", topic:"Generics", q:"Why can't you create a generic array: new T[]?", opts:["Syntax error only","Type erasure: at runtime T is Object. Arrays are covariant and reifiable (retain type at runtime). Generic arrays would break type safety — use List<T> instead","Memory limitation","Can in Java 11+"], ans:1, exp:"Arrays check element type at runtime (ArrayStoreException). Generics are erased at runtime. If T[] were allowed, you could store wrong types without detection → heap pollution." },
  { id:"java83", diff:"hard", topic:"Generics", q:"What is a bounded type parameter? Give an example.", opts:["A parameter with a default value","<T extends Comparable<T>> constrains T to types that implement Comparable. Enables calling compareTo() on T within the method","A final type parameter","A parameter limited to primitives"], ans:1, exp:"<T extends Number> allows only Number subclasses. <T extends Comparable<T> & Serializable> allows multiple bounds (one class + interfaces). Enables compiler to know available methods." },
  { id:"java84", diff:"scenario", topic:"Generics", q:"Scenario: What is the output?\nList<String> list = new ArrayList<>();\nList rawList = list;\nrawList.add(42);\nString s = list.get(0);", opts:["42","Compile warning but runs, then ClassCastException on the last line","Compile error","null"], ans:1, exp:"Raw type bypasses generics. add(42) succeeds (no type check). list.get(0) auto-casts to String → ClassCastException at runtime. This is why raw types are dangerous — heap pollution." },

  // ═══ MULTITHREADING (8) ═══
  { id:"java85", diff:"easy", topic:"Multithreading", q:"What are the two ways to create a thread in Java?", opts:["Only extends Thread","Extend Thread class and override run(), OR implement Runnable interface and pass to Thread constructor. Runnable is preferred (allows extending other classes)","Only implement Runnable","Use static methods"], ans:1, exp:"Runnable is preferred: (1) Java supports single inheritance, extending Thread wastes it. (2) Runnable separates task from thread. Java 8: Runnable via lambda. Also: Callable<V> returns a value." },
  { id:"java86", diff:"medium", topic:"Multithreading", q:"What is the difference between start() and run()?", opts:["Same thing","start(): creates new OS thread, which calls run() in that thread. Calling run() directly executes it in the current thread — no new thread is created","run() creates thread","start() blocks"], ans:1, exp:"thread.start() → JVM creates a new thread → calls run() in new thread context. thread.run() → runs in caller's thread like any normal method. Classic interview gotcha." },
  { id:"java87", diff:"medium", topic:"Multithreading", q:"What is the 'synchronized' keyword?", opts:["Makes code faster","Ensures only one thread can execute the synchronized block/method at a time — mutual exclusion using the object's intrinsic lock (monitor)","Makes variables volatile","Prevents deadlocks"], ans:1, exp:"synchronized method: locks 'this' (or Class for static). synchronized(obj) block: locks specified object. Only one thread holds the lock at a time. Others block. Prevents race conditions." },
  { id:"java88", diff:"medium", topic:"Multithreading", q:"What is the difference between volatile and synchronized?", opts:["Same thing","volatile: ensures visibility (reads/writes go to main memory, no caching). synchronized: ensures both visibility AND atomicity (mutual exclusion). volatile doesn't prevent race conditions on compound operations (i++)","volatile is stronger","synchronized is for variables only"], ans:1, exp:"volatile int x: all threads see latest x value. But x++ (read-modify-write) is NOT atomic even with volatile. Use AtomicInteger for lock-free atomic operations, or synchronized for compound actions." },
  { id:"java89", diff:"hard", topic:"Multithreading", q:"What is a deadlock? Give conditions.", opts:["A slow thread","Two+ threads each hold a lock and wait for the other's lock — creating a circular wait. Four conditions: mutual exclusion, hold-and-wait, no preemption, circular wait","A crashed thread","An infinite loop"], ans:1, exp:"Thread A holds lock1, waits for lock2. Thread B holds lock2, waits for lock1. Prevention: acquire locks in consistent order, use tryLock() with timeout, avoid nested locking." },
  { id:"java90", diff:"scenario", topic:"Multithreading", q:"Scenario: What is the output of i after two threads each increment it 1000 times? (int i = 0; not synchronized)", opts:["Always 2000","Unpredictable — less than or equal to 2000 due to race condition (i++ is not atomic: read → increment → write can interleave)","Always 1000","Compile error"], ans:1, exp:"i++ is three operations: read i, add 1, write i. Two threads can read the same value, both increment to same result → lost update. Fix: synchronized, AtomicInteger, or volatile (not enough alone for i++)." },
  { id:"java91", diff:"scenario", topic:"Multithreading", q:"Scenario: Thread A calls wait() on an object. What must it hold?", opts:["Nothing","The object's intrinsic lock (monitor) — wait/notify must be called inside synchronized block/method on the same object","A Semaphore","A ReentrantLock"], ans:1, exp:"wait() releases the lock and puts thread in WAITING state. notify() wakes one waiting thread. Both MUST be called inside synchronized(obj) or synchronized method. Otherwise: IllegalMonitorStateException." },

  // ═══ MEMORY MODEL & GC (6) ═══
  { id:"java92", diff:"medium", topic:"Memory", q:"What is the difference between Stack and Heap memory in Java?", opts:["Same area","Stack: per-thread, stores local variables and method call frames (fast, LIFO). Heap: shared, stores all objects created with 'new' (GC-managed, slower)","Stack stores objects","Heap is per-thread"], ans:1, exp:"Stack: method frames (local variables, references), primitive values. Fast, auto-freed on method exit. Heap: all objects (new), shared across threads, managed by Garbage Collector. StackOverflowError vs OutOfMemoryError." },
  { id:"java93", diff:"medium", topic:"Memory", q:"What is garbage collection in Java?", opts:["Manual memory deallocation","Automatic process that identifies and reclaims objects no longer referenced — developers don't need to free memory manually","Clearing the stack","Defragmenting disk"], ans:1, exp:"GC traces from root references (stack variables, static fields). Objects not reachable from any root are eligible for GC. Java uses generational GC: Young Gen (Eden/Survivor) and Old Gen." },
  { id:"java94", diff:"medium", topic:"Memory", q:"When is an object eligible for garbage collection?", opts:["When it goes out of scope","When there are no reachable references to it from any GC root (local variables, static fields, active threads)","When finalize() is called","After System.gc()"], ans:1, exp:"Eligible when unreachable: no live thread can access it via any chain of references. Setting reference to null doesn't guarantee immediate GC — just makes it eligible. System.gc() is only a suggestion." },
  { id:"java95", diff:"hard", topic:"Memory", q:"What is the String Pool and where does it reside?", opts:["In the Stack","In the Heap (since Java 7) — a special area that caches String literals for reuse. Before Java 7, it was in PermGen","In PermGen always","Outside JVM memory"], ans:1, exp:"Java 6: String Pool in PermGen (fixed size, could cause OOM). Java 7+: moved to main Heap (GC can collect unused interned strings, larger space). String.intern() adds to pool." },
  { id:"java96", diff:"scenario", topic:"Memory", q:"Scenario: What causes a memory leak in Java?", opts:["Not possible with GC","Objects are referenced but no longer needed — GC can't reclaim them. Examples: unclosed resources, static collections growing endlessly, listeners not deregistered, inner class holding outer reference","Only in native code","Only with finalize()"], ans:1, exp:"GC prevents most leaks but can't fix logical leaks: a static HashMap that grows forever, event listeners never removed, non-static inner class preventing outer class collection, ThreadLocal not cleared." },
  { id:"java97", diff:"scenario", topic:"Memory", q:"Scenario: StackOverflowError vs OutOfMemoryError — when does each occur?", opts:["Same thing","StackOverflow: too many recursive calls exhaust thread stack. OutOfMemory: heap is full (too many objects) or GC overhead exceeds 98% of time","StackOverflow is a warning","OutOfMemory is recoverable"], ans:1, exp:"StackOverflow: usually infinite/deep recursion. Fix: convert to iterative, or increase -Xss. OutOfMemory: heap exhausted. Fix: fix memory leak, increase -Xmx, use more efficient data structures." },

  // ═══ FUNCTIONAL PROGRAMMING & STREAMS (6) ═══
  { id:"java98", diff:"easy", topic:"Lambdas", q:"What is a functional interface?", opts:["Interface with many methods","An interface with exactly one abstract method — can be used as the target for lambda expressions. Annotated with @FunctionalInterface","An interface with no methods","A deprecated concept"], ans:1, exp:"Examples: Runnable (run()), Callable (call()), Comparator (compare()), Function<T,R> (apply()), Predicate<T> (test()), Consumer<T> (accept()), Supplier<T> (get()). Lambda provides implementation inline." },
  { id:"java99", diff:"medium", topic:"Lambdas", q:"What is a lambda expression?", opts:["A new class type","A concise way to implement a functional interface: (params) -> expression. Replaces verbose anonymous inner classes","A static method","A design pattern"], ans:1, exp:"Old: new Comparator<String>() { public int compare(String a, String b) { return a.compareTo(b); } }. Lambda: (a, b) -> a.compareTo(b). Method reference: String::compareTo." },
  { id:"java100", diff:"medium", topic:"Streams", q:"What is the difference between intermediate and terminal operations in Streams?", opts:["Same thing","Intermediate (filter, map, sorted): lazy, return Stream, chained. Terminal (collect, forEach, count, reduce): trigger execution, return result or side-effect","Both are lazy","Both trigger execution"], ans:1, exp:"Intermediate ops are lazy — nothing happens until a terminal op is called. Stream pipeline: source → intermediate ops (lazy) → terminal op (triggers processing). forEach is terminal." },
  { id:"java101", diff:"medium", topic:"Streams", q:"What is the output?\nList.of(1,2,3,4,5).stream()\n  .filter(n -> n > 2)\n  .map(n -> n * 2)\n  .forEach(System.out::print);", opts:["246810","6810","345","12345"], ans:1, exp:"filter(n>2) → [3,4,5]. map(n*2) → [6,8,10]. forEach prints each → 6810. Stream processing: lazy evaluation, elements pass through pipeline one by one." },
  { id:"java102", diff:"hard", topic:"Streams", q:"What is the difference between map() and flatMap()?", opts:["Same thing","map(): transforms each element 1→1 (T→R). flatMap(): transforms each element 1→many and flattens (T→Stream<R>) — useful for nested collections","flatMap is slower","map handles nulls"], ans:1, exp:"map: Stream<List<String>> → Stream<List<String>> (wrapped). flatMap: Stream<List<String>> → Stream<String> (flattened). Example: flatten list of lists: lists.stream().flatMap(List::stream)." },
  { id:"java103", diff:"scenario", topic:"Streams", q:"Scenario: You need to find the sum of squares of even numbers in a list. Stream approach?", opts:["for loop only","list.stream().filter(n -> n%2==0).map(n -> n*n).reduce(0, Integer::sum)","Sort then sum","Use PriorityQueue"], ans:1, exp:"filter(even) → map(square) → reduce(sum). Or use mapToInt() + sum(): list.stream().filter(n->n%2==0).mapToInt(n->n*n).sum(). Declarative, readable, parallelizable." },

  // ═══ IMPORTANT APIs & CONCEPTS (7) ═══
  { id:"java104", diff:"medium", topic:"Object Class", q:"What methods does every Java class inherit from Object?", opts:["Only toString()","equals(), hashCode(), toString(), getClass(), clone(), finalize(), wait(), notify(), notifyAll()","Only equals and hashCode","No methods"], ans:1, exp:"Object is the root. Key methods: equals() (logical equality), hashCode() (hash for collections), toString() (string representation), clone() (copying), finalize() (deprecated cleanup)." },
  { id:"java105", diff:"medium", topic:"Object Class", q:"Why is it important to override toString()?", opts:["Not important","Default toString() returns className@hashCode (useless). Overriding provides meaningful output for debugging, logging, and readability","Required by compiler","Improves performance"], ans:1, exp:"Default: 'com.example.Employee@1a2b3c'. Override: 'Employee{name='John', salary=50000}'. Used in System.out.println(), logging, debugger. Always override for domain objects." },
  { id:"java106", diff:"medium", topic:"Wrapper Classes", q:"What is Integer.valueOf() vs new Integer()?", opts:["Same thing","valueOf() uses cache (-128 to 127), returning existing objects. new Integer() always creates a new object (deprecated since Java 9)","valueOf is slower","new is cached"], ans:1, exp:"valueOf(127) returns cached Integer → same reference for same value. new Integer(127) creates fresh object (deprecated). Autoboxing uses valueOf() internally. Always prefer valueOf()." },
  { id:"java107", diff:"hard", topic:"Immutability", q:"How does String achieve immutability?", opts:["Using synchronized","final class (can't extend), private final char[] (Java 8) or byte[] (Java 9+), no methods modify the internal array, returns new String for any modification","Using volatile","Using transient"], ans:1, exp:"String is final (no subclass override). Internal value array is private final. No setter methods. Any 'modification' (concat, substring, replace) returns a new String. hashCode is cached." },
  { id:"java108", diff:"scenario", topic:"Lambdas", q:"Scenario: What is a method reference and when to use it?", opts:["A pointer to memory","Shorthand for lambda that calls an existing method. Types: Static (Math::abs), Instance (str::length), Arbitrary instance (String::toLowerCase), Constructor (ArrayList::new)","A deprecated feature","Same as reflection"], ans:1, exp:"Instead of x -> Math.abs(x), write Math::abs. Cleaner when lambda just delegates to an existing method. Compiler infers the functional interface from context." },
  { id:"java109", diff:"scenario", topic:"Object Class", q:"Scenario: You put two equal Employee objects (same id) in a HashSet. Both appear. What went wrong?", opts:["HashSet allows duplicates","equals() was overridden but hashCode() was NOT. Objects go to different buckets → HashSet thinks they're different despite equals() returning true","HashSet is broken","equals was not overridden"], ans:1, exp:"HashSet uses hashCode() first to find bucket, then equals() to check within bucket. If hashCode() isn't overridden, it uses Object's default (memory address) → different buckets → 'duplicate' stored." },
  { id:"java110", diff:"scenario", topic:"Collections", q:"Scenario: What is the output?\nMap<String,Integer> map = new HashMap<>();\nmap.put(\"a\", 1);\nmap.put(\"b\", 2);\nmap.put(\"a\", 3);\nSystem.out.println(map);", opts:["{a=1, b=2, a=3}","{a=3, b=2} — duplicate key 'a' is overwritten with new value 3","{a=1, b=2}","ConcurrentModificationException"], ans:1, exp:"HashMap keys are unique. put(\"a\", 3) finds existing key \"a\" and replaces its value (1→3). put() returns the old value (1). The map has 2 entries: {a=3, b=2}." }
];
