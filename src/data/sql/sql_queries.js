export const SQL_QUERIES_Q = [

  // ─── BASIC ───────────────────────────────────────────────────────────────────
  {id:"sqlq1",diff:"easy",topic:"Queries",q:"What is the output of: SELECT 10 + '5'; (in MySQL)?",opts:["Error","'105'","15","'15'"],ans:2,exp:"MySQL implicitly converts the string '5' to an integer, so 10 + '5' evaluates to 15."},
  {id:"sqlq2",diff:"easy",topic:"Queries",q:"Which query retrieves rows where 'Age' is between 20 and 30 (inclusive)?",opts:["SELECT * FROM T WHERE Age > 20 AND Age < 30;","SELECT * FROM T WHERE Age BETWEEN 20 AND 30;","SELECT * FROM T WHERE Age IN (20,30);","SELECT * FROM T WHERE Age >= 20 OR Age <= 30;"],ans:1,exp:"BETWEEN is inclusive on both ends, equivalent to Age >= 20 AND Age <= 30."},
  {id:"sqlq3",diff:"easy",topic:"Queries",q:"Which query finds all employees whose name starts with 'A'?",opts:["SELECT * FROM Emp WHERE Name = 'A%';","SELECT * FROM Emp WHERE Name LIKE 'A%';","SELECT * FROM Emp WHERE Name STARTS WITH 'A';","SELECT * FROM Emp WHERE Name IN ('A%');"],ans:1,exp:"The LIKE operator with the '%' wildcard matches zero or more characters after 'A'."},
  {id:"sqlq4",diff:"easy",topic:"Queries",q:"What does SELECT UPPER('hello'); return?",opts:["hello","HELLO","Hello","Error"],ans:1,exp:"UPPER() converts all characters in a string to uppercase."},
  {id:"sqlq5",diff:"easy",topic:"Queries",q:"Which query counts only non-NULL values in column 'Score'?",opts:["SELECT COUNT(*) FROM T;","SELECT COUNT(Score) FROM T;","SELECT SUM(Score) FROM T;","SELECT TOTAL(Score) FROM T;"],ans:1,exp:"COUNT(column_name) ignores NULL values and counts only non-NULL entries."},
  {id:"sqlq6",diff:"easy",topic:"Queries",q:"How do you alias a column 'emp_name' as 'Employee Name' in the result?",opts:["SELECT emp_name Employee Name FROM Emp;","SELECT emp_name AS 'Employee Name' FROM Emp;","SELECT emp_name = 'Employee Name' FROM Emp;","RENAME emp_name TO 'Employee Name';"],ans:1,exp:"The AS keyword creates a column alias. Aliases with spaces must be wrapped in quotes."},
  {id:"sqlq7",diff:"easy",topic:"Queries",q:"What is the correct syntax to insert a NULL value into a column?",opts:["INSERT INTO T(col) VALUES('NULL');","INSERT INTO T(col) VALUES(NULL);","INSERT INTO T(col) VALUES(0);","INSERT INTO T VALUES(EMPTY);"],ans:1,exp:"NULL (without quotes) represents the absence of a value. 'NULL' (with quotes) would be treated as the string 'NULL'."},
  {id:"sqlq8",diff:"easy",topic:"Queries",q:"Which query retrieves the last 5 records from a table ordered by 'Id' ascending?",opts:["SELECT * FROM T ORDER BY Id ASC LIMIT 5;","SELECT * FROM T ORDER BY Id DESC LIMIT 5;","SELECT LAST 5 FROM T ORDER BY Id;","SELECT * FROM T WHERE Id > (SELECT MAX(Id)-5 FROM T);"],ans:1,exp:"Ordering DESC and taking LIMIT 5 gives the 5 highest IDs, which are the last 5 records when the table is sorted ascending."},
  {id:"sqlq9",diff:"easy",topic:"Queries",q:"What does SELECT LENGTH('database'); return?",opts:["7","8","9","Error"],ans:1,exp:"'database' has 8 characters (d-a-t-a-b-a-s-e), so LENGTH() returns 8."},
  {id:"sqlq10",diff:"easy",topic:"Queries",q:"Which SQL clause limits the number of rows returned by a query?",opts:["TOP","LIMIT","ROWNUM","All of the above (dialect-specific)"],ans:3,exp:"LIMIT is MySQL/PostgreSQL syntax, TOP is SQL Server syntax, and ROWNUM is Oracle syntax. They all serve the same purpose."},

  // ─── INTERMEDIATE ─────────────────────────────────────────────────────────────
  {id:"sqlq11",diff:"medium",topic:"Queries",q:"What is the result of: SELECT COALESCE(NULL, NULL, 3, 4);?",opts:["NULL","0","3","4"],ans:2,exp:"COALESCE returns the first non-NULL value. NULL and NULL are skipped; 3 is returned."},
  {id:"sqlq12",diff:"medium",topic:"Queries",q:"Which query swaps the values of 'FirstName' and 'LastName' columns without a temp column?",opts:["UPDATE T SET FirstName=LastName, LastName=FirstName;","SWAP T.FirstName WITH T.LastName;","UPDATE T SET FirstName=LastName;UPDATE T SET LastName=FirstName;","UPDATE T SET (FirstName,LastName)=(LastName,FirstName);"],ans:0,exp:"SQL evaluates the right-hand side using the original values before any update, so setting both columns simultaneously effectively swaps them."},
  {id:"sqlq13",diff:"medium",topic:"Queries",q:"What does: SELECT SUBSTRING('QuizApp', 5, 3); return?",opts:["Qui","App","zAp","pAp"],ans:1,exp:"SUBSTRING(str, start, length): starts at position 5 ('A') and takes 3 characters → 'App'."},
  {id:"sqlq14",diff:"medium",topic:"Queries",q:"Which query finds employees with NULL in the 'Department' column?",opts:["SELECT * FROM Emp WHERE Department = NULL;","SELECT * FROM Emp WHERE Department IS NULL;","SELECT * FROM Emp WHERE Department == NULL;","SELECT * FROM Emp WHERE ISNULL(Department) = 1;"],ans:1,exp:"You cannot use = to compare NULLs because NULL = NULL evaluates to UNKNOWN, not TRUE. IS NULL is the correct operator."},
  {id:"sqlq15",diff:"medium",topic:"Queries",q:"What is the output of: SELECT ROUND(15.654, 1);?",opts:["15.6","15.7","16.0","15.65"],ans:1,exp:"ROUND(15.654, 1) rounds to 1 decimal place. Since the second decimal (5) is >= 5, it rounds up to 15.7."},
  {id:"sqlq16",diff:"medium",topic:"Queries",q:"Which query retrieves all orders placed in the year 2024?",opts:["SELECT * FROM Orders WHERE YEAR(OrderDate) = 2024;","SELECT * FROM Orders WHERE OrderDate = 2024;","SELECT * FROM Orders WHERE OrderDate LIKE '2024%';","Both A and C are valid approaches"],ans:3,exp:"YEAR() is a clean function approach. LIKE '2024%' works if the date is stored as a string or you cast it. Both are commonly used."},
  {id:"sqlq17",diff:"medium",topic:"Queries",q:"What does GROUP_CONCAT (MySQL) or STRING_AGG (PostgreSQL/SQL Server) do?",opts:["Sums a group of strings","Concatenates values from multiple rows of a group into a single string","Finds the longest string in a group","Counts distinct string values in a group"],ans:1,exp:"These aggregate functions collect all string values within a group and concatenate them, often with a separator. E.g., 'Alice,Bob,Charlie'."},
  {id:"sqlq18",diff:"medium",topic:"Queries",q:"Which query finds duplicate emails in a 'Users' table?",opts:["SELECT Email FROM Users WHERE Email = Email;","SELECT Email, COUNT(*) FROM Users GROUP BY Email HAVING COUNT(*) > 1;","SELECT DISTINCT Email FROM Users;","SELECT Email FROM Users ORDER BY Email HAVING DUPLICATE = TRUE;"],ans:1,exp:"GROUP BY Email groups rows with the same email, COUNT(*) counts them, and HAVING COUNT(*) > 1 filters only those appearing more than once."},
  {id:"sqlq19",diff:"medium",topic:"Queries",q:"What is the output of: SELECT MOD(17, 5);?",opts:["3","2","4","3.4"],ans:1,exp:"MOD(17, 5) returns the remainder of 17 / 5. 5 * 3 = 15, so remainder = 17 - 15 = 2."},
  {id:"sqlq20",diff:"medium",topic:"Queries",q:"Which query correctly uses a CASE expression to label salaries?",opts:["SELECT Name, IF Salary > 50000 THEN 'High' ELSE 'Low' FROM Emp;","SELECT Name, CASE WHEN Salary > 50000 THEN 'High' ELSE 'Low' END AS SalaryBand FROM Emp;","SELECT Name, SWITCH(Salary > 50000, 'High', 'Low') FROM Emp;","SELECT Name, IIF(Salary > 50000, 'High', 'Low') FROM Emp;"],ans:1,exp:"CASE WHEN...THEN...ELSE...END is standard ANSI SQL and supported across all major databases. IIF is SQL Server-specific; IF-THEN is PL/SQL."},
  {id:"sqlq21",diff:"medium",topic:"Queries",q:"You run: UPDATE Emp SET Salary = Salary * 1.10; with no WHERE clause. What happens?",opts:["Error: WHERE clause is mandatory","Only the first row is updated","All rows in the Emp table get a 10% salary increase","The query is rolled back automatically"],ans:2,exp:"Without a WHERE clause, an UPDATE statement modifies every single row in the table. Always double-check before running an unfiltered UPDATE in production."},
  {id:"sqlq22",diff:"medium",topic:"Queries",q:"What does the TRIM() function do?",opts:["Removes all spaces from a string","Removes leading and trailing whitespace from a string","Truncates a string to a given length","Removes a specific character from the middle of a string"],ans:1,exp:"TRIM() removes leading and trailing spaces. LTRIM() removes only leading, RTRIM() removes only trailing spaces."},

  // ─── ADVANCED ────────────────────────────────────────────────────────────────
  {id:"sqlq23",diff:"hard",topic:"Queries",q:"What does this query do? SELECT *, ROW_NUMBER() OVER (PARTITION BY Email ORDER BY Id DESC) AS rn FROM Users;",opts:["Deletes duplicate emails","Numbers each row; rn=1 indicates the most recent record per email","Finds the oldest record per email","Creates a view of unique emails"],ans:1,exp:"ROW_NUMBER() resets to 1 for each unique Email (PARTITION BY) and numbers rows newest-first (ORDER BY Id DESC). Wrapping this in a CTE and filtering WHERE rn=1 deduplicates the table."},
  {id:"sqlq24",diff:"hard",topic:"Queries",q:"What is a RECURSIVE CTE used for?",opts:["CTEs that call stored procedures","CTEs that reference themselves to iterate through hierarchical data (e.g., org charts, file trees)","CTEs that auto-refresh like materialized views","CTEs that are cached for reuse across multiple queries"],ans:1,exp:"A recursive CTE has an anchor member (base case) and a recursive member that references the CTE itself, enabling traversal of parent-child hierarchies like employee→manager trees."},
  {id:"sqlq25",diff:"hard",topic:"Queries",q:"Which query correctly uses LEAD() to find the next order date for each customer?",opts:["SELECT OrderId, LEAD(OrderDate) FROM Orders GROUP BY CustomerId;","SELECT OrderId, CustomerId, OrderDate, LEAD(OrderDate,1) OVER (PARTITION BY CustomerId ORDER BY OrderDate) AS NextOrder FROM Orders;","SELECT OrderId, NEXT(OrderDate) OVER (CustomerId) FROM Orders;","SELECT OrderId, LAG(OrderDate) OVER (ORDER BY OrderDate DESC) FROM Orders;"],ans:1,exp:"LEAD(col, offset) OVER (PARTITION BY ... ORDER BY ...) peeks at the next row's value within each partition. Partitioning by CustomerId ensures we stay within each customer's orders."},
  {id:"sqlq26",diff:"hard",topic:"Queries",q:"What is the difference between WHERE and HAVING in terms of query execution order?",opts:["No difference; both filter rows the same way","WHERE filters rows BEFORE grouping; HAVING filters groups AFTER aggregation","HAVING filters rows BEFORE grouping; WHERE filters AFTER","WHERE is for single tables; HAVING is for joined tables"],ans:1,exp:"SQL logical order: FROM → WHERE (row filter) → GROUP BY → HAVING (group filter) → SELECT → ORDER BY. This is why WHERE cannot reference aggregate functions."},
  {id:"sqlq27",diff:"hard",topic:"Queries",q:"What does EXPLAIN SELECT * FROM Orders WHERE CustomerId=5; primarily tell you?",opts:["It runs the query and explains the output","Whether the query uses an index or a full table scan, and the estimated cost","The number of rows in the Orders table","Whether CustomerId is a primary key"],ans:1,exp:"EXPLAIN shows the query execution plan: access type (index range scan, full scan), which index is used, estimated rows, and cost. Essential for query optimization."},
  {id:"sqlq28",diff:"hard",topic:"Queries",q:"Which query pivots row data to columns — showing total sales per product as columns?",opts:["GROUP BY Product ORDER BY Sales","Using CASE WHEN with conditional aggregation: SUM(CASE WHEN Product='A' THEN Sales END) AS A","Using PIVOT keyword (SQL Server/Oracle specific)","Both B and C are valid approaches"],ans:3,exp:"PIVOT keyword exists in SQL Server/Oracle. In MySQL/PostgreSQL, conditional aggregation (CASE WHEN inside SUM/COUNT) is the portable way to pivot rows into columns."},
  {id:"sqlq29",diff:"hard",topic:"Queries",q:"What does this return? SELECT NULLIF(5, 5);",opts:["5","0","NULL","Error"],ans:2,exp:"NULLIF(a, b) returns NULL when a equals b. Since 5 = 5, the result is NULL. Useful for preventing divide-by-zero: value / NULLIF(denominator, 0)."},
  {id:"sqlq30",diff:"hard",topic:"Queries",q:"You need rows from Table A that do NOT exist in Table B (anti-join). Which approach avoids NULL-related bugs?",opts:["WHERE id NOT IN (SELECT id FROM B)","LEFT JOIN B ON A.id=B.id WHERE B.id IS NULL","WHERE NOT EXISTS (SELECT 1 FROM B WHERE B.id=A.id)","Both B and C; avoid NOT IN if B.id can contain NULLs"],ans:3,exp:"NOT IN returns no rows at all if the subquery has even one NULL. LEFT JOIN...IS NULL and NOT EXISTS are safe, reliable anti-join patterns."},
  {id:"sqlq31",diff:"hard",topic:"Queries",q:"A query uses: SELECT ... FROM A JOIN B ON A.id=B.id JOIN C ON B.ref=C.ref WHERE C.status='active'. The optimizer may choose to join tables in which order?",opts:["Always A → B → C as written","The optimizer freely reorders joins based on statistics and costs","Always smallest table first","It depends on which table is mentioned first in FROM"],ans:1,exp:"The SQL query optimizer can reorder joins in any sequence that produces the same result, guided by table statistics, index availability, and cost estimates."},
  {id:"sqlq32",diff:"hard",topic:"Queries",q:"What does this query find? SELECT DeptId, AVG(Salary) FROM Emp GROUP BY DeptId HAVING AVG(Salary) > (SELECT AVG(Salary) FROM Emp);",opts:["Departments where any employee earns above average","Departments whose average salary is above the overall company average salary","The company's average salary","All employees earning above average"],ans:1,exp:"The subquery computes the company-wide average. HAVING compares each department's average against it, returning only departments that exceed the global average."},

  // ─── SCENARIO-BASED QUERIES ──────────────────────────────────────────────────
  {id:"sqlq33",diff:"scenario",topic:"Query Scenario",q:"SCENARIO: Table 'Scores(StudentId, Subject, Marks)'. Write a query to find the highest-scoring student per subject.",opts:["SELECT Subject, MAX(Marks) FROM Scores GROUP BY Subject;","SELECT s.StudentId, s.Subject, s.Marks FROM Scores s WHERE s.Marks = (SELECT MAX(Marks) FROM Scores WHERE Subject = s.Subject);","SELECT StudentId, Subject, RANK() OVER (PARTITION BY Subject ORDER BY Marks DESC) AS r FROM Scores WHERE r=1;","SELECT StudentId, Subject, Marks FROM Scores ORDER BY Marks DESC LIMIT 1;"],ans:1,exp:"A correlated subquery finds the MAX(Marks) for each subject and the outer query returns the student matching that mark per subject. Window functions (RANK/DENSE_RANK) wrapped in a CTE also solve this cleanly."},
  {id:"sqlq34",diff:"scenario",topic:"Query Scenario",q:"SCENARIO: 'Transactions(id, user_id, amount, created_at)'. Find users whose total spending in the last 30 days exceeds $1000.",opts:["SELECT user_id FROM Transactions WHERE amount > 1000;","SELECT user_id, SUM(amount) FROM Transactions WHERE created_at >= NOW() - INTERVAL 30 DAY GROUP BY user_id HAVING SUM(amount) > 1000;","SELECT user_id FROM Transactions GROUP BY user_id WHERE SUM(amount) > 1000;","SELECT user_id FROM Transactions HAVING SUM(amount) > 1000 WHERE DATE > 30;"],ans:1,exp:"Filter rows by date range with WHERE, aggregate per user with GROUP BY, then filter groups by total spend with HAVING. Note: WHERE must come before GROUP BY."},
  {id:"sqlq35",diff:"scenario",topic:"Query Scenario",q:"SCENARIO: 'Employees(id, name, manager_id)' — manager_id references the same table. Write a query to list each employee alongside their manager's name.",opts:["SELECT e.name, m.name FROM Employees e, Employees m WHERE e.manager_id = m.id;","SELECT e.name AS Employee, m.name AS Manager FROM Employees e LEFT JOIN Employees m ON e.manager_id = m.id;","SELECT e.name FROM Employees e JOIN Employees m ON e.id = m.manager_id;","SELECT name, (SELECT name FROM Employees WHERE id = manager_id) AS Manager FROM Employees;"],ans:1,exp:"A self-join with LEFT JOIN handles the CEO (whose manager_id is NULL) by still returning them with a NULL manager name. Both B and D work, but LEFT JOIN is more readable and standard."},
  {id:"sqlq36",diff:"scenario",topic:"Query Scenario",q:"SCENARIO: 'PageViews(page_url, view_date)'. Find pages that had views on EVERY day in the last 7 days.",opts:["SELECT page_url FROM PageViews WHERE view_date >= CURDATE()-7;","SELECT page_url FROM PageViews GROUP BY page_url HAVING COUNT(DISTINCT view_date) = 7;","SELECT DISTINCT page_url FROM PageViews WHERE COUNT(view_date) = 7;","SELECT page_url FROM PageViews HAVING view_date IN (LAST 7 DAYS);"],ans:1,exp:"GROUP BY page_url then HAVING COUNT(DISTINCT view_date) = 7 ensures the page had activity on all 7 distinct days. Add a WHERE clause to filter to just the last 7 days first."},
  {id:"sqlq37",diff:"scenario",topic:"Query Scenario",q:"SCENARIO: 'Orders(order_id, customer_id, status, amount)'. Calculate the percentage of total revenue contributed by each customer.",opts:["SELECT customer_id, SUM(amount)/COUNT(*)*100 FROM Orders GROUP BY customer_id;","SELECT customer_id, SUM(amount) * 100.0 / (SELECT SUM(amount) FROM Orders) AS pct FROM Orders GROUP BY customer_id;","SELECT customer_id, SUM(amount) OVER() / SUM(amount) FROM Orders;","SELECT customer_id, amount / SUM(amount) * 100 FROM Orders GROUP BY customer_id;"],ans:1,exp:"The subquery computes total revenue. Each customer's SUM(amount) is divided by it and multiplied by 100. Alternatively: SUM(amount) * 100.0 / SUM(SUM(amount)) OVER() using a window function."},
  {id:"sqlq38",diff:"scenario",topic:"Query Scenario",q:"SCENARIO: 'Events(user_id, event_type, event_time)'. Find users who performed event 'login' followed by event 'purchase' within 1 hour.",opts:["SELECT DISTINCT user_id FROM Events WHERE event_type IN ('login','purchase');","SELECT DISTINCT l.user_id FROM Events l JOIN Events p ON l.user_id=p.user_id WHERE l.event_type='login' AND p.event_type='purchase' AND p.event_time BETWEEN l.event_time AND l.event_time + INTERVAL 1 HOUR;","SELECT user_id FROM Events GROUP BY user_id HAVING event_type='login' AND event_type='purchase';","SELECT user_id FROM Events WHERE event_type='login' AND LEAD(event_type)='purchase';"],ans:1,exp:"Self-joining the Events table lets you correlate a login event (l) with a purchase event (p) for the same user within a 1-hour window using a time-range condition."},
];
