export const OS_Q5 = [
  // ═══ ADVANCED OS CONCEPTS & SCENARIOS (30) ═══
  { id: "os91", diff: "medium", topic: "Process Management", q: "In a Unix/Linux system, what is the role of the `init` or `systemd` process?", opts: ["To format the hard drive", "It is the first process started by the kernel (PID 1) and acts as the ancestor of all other user-space processes", "It handles memory management", "It is responsible for networking only"], ans: 1, exp: "PID 1 is the ultimate parent. If a process's parent dies, it gets reparented to PID 1, which eventually reaps its zombie state." },
  { id: "os92", diff: "hard", topic: "CPU Scheduling", q: "What is the primary difference between a Context Switch and a Mode Switch?", opts: ["There is no difference", "A Context Switch changes the running process, while a Mode Switch only changes privilege level (user to kernel) within the same process", "A Mode Switch happens only during reboot", "A Context Switch is hardware-based, Mode Switch is software-based"], ans: 1, exp: "A system call triggers a mode switch to kernel mode, but the CPU is still executing on behalf of the same process. A context switch replaces the PCB and virtual memory map entirely to run a different process." },
  { id: "os93", diff: "medium", topic: "Synchronization", q: "Which hardware instruction is heavily relied upon to implement Spinlocks and Mutexes safely on multi-core systems?", opts: ["JMP", "Test-And-Set (or Compare-And-Swap)", "HALT", "NOP"], ans: 1, exp: "Test-And-Set (TAS) or Compare-And-Swap (CAS) are atomic hardware instructions that read a memory location and write to it in a single, uninterruptible clock cycle, preventing race conditions during lock acquisition." },
  { id: "os94", diff: "easy", topic: "Synchronization", q: "What is a Spinlock?", opts: ["A lock that spins the hard drive", "A lock where a thread waiting for it continuously loops (spins) checking if it's available, consuming CPU cycles", "A lock that immediately puts the thread to sleep", "A lock exclusively for disk I/O"], ans: 1, exp: "Spinlocks avoid the overhead of a context switch (putting a thread to sleep and waking it up). They are useful only if the lock will be held for a very short duration on a multi-core system." },
  { id: "os95", diff: "hard", topic: "Memory Management", q: "What is the 'Working Set' model?", opts: ["A model for employee shifts", "A dynamic memory management model that tracks the set of pages a process is actively using over a recent time window to prevent thrashing", "A CPU scheduling metric", "A file system structure"], ans: 1, exp: "The OS tracks the 'Working Set' (the pages recently accessed by a process). If the total size of all working sets exceeds physical memory, thrashing will occur, so the OS suspends a process." },
  { id: "os96", diff: "medium", topic: "Memory Management", q: "In a Virtual Memory system, what does the 'Dirty Bit' (Modify Bit) in a page table entry signify?", opts: ["The page contains a virus", "The page has been modified (written to) since being loaded from disk, so it must be written back to disk before its frame can be evicted", "The page is read-only", "The page belongs to the kernel"], ans: 1, exp: "If a page hasn't been modified, the OS can just discard it when evicting because the disk copy is still identical. If it's 'dirty', it must incur the cost of a disk write to save changes." },
  { id: "os97", diff: "medium", topic: "File Systems", q: "What does a Journaling File System (like ext4 or NTFS) do?", opts: ["It tracks user browsing history", "It keeps a log (journal) of intended metadata changes before writing them to the main file system, allowing for extremely fast recovery after a crash", "It encrypts files automatically", "It compresses files"], ans: 1, exp: "Journaling prevents file system corruption. After an unexpected power loss, the OS simply replays the journal to ensure all partially written metadata is fully applied or cleanly rolled back." },
  { id: "os98", diff: "hard", topic: "I/O Systems", q: "What is Memory-Mapped I/O?", opts: ["Using a map to find devices", "Device registers and buffers are mapped directly into the CPU's memory address space. The CPU uses standard load/store memory instructions to communicate with the device", "Allocating RAM to the hard drive", "Storing files in RAM"], ans: 1, exp: "Instead of having special I/O instructions (like `in` and `out`), the device control registers are mapped to specific physical memory addresses. Writing to that memory address sends a command to the device." },
  { id: "os99", diff: "scenario", topic: "Memory Management", q: "Scenario: Two completely unrelated processes, A and B, want to share a 1MB region of memory for IPC. How does the OS handle this under the hood?", opts: ["The OS copies 1MB back and forth on every read/write", "The OS modifies the Page Tables of both Process A and Process B so that specific logical pages in both processes map to the exact same physical frames in RAM", "The OS disables memory protection completely", "The OS routes the data through the network stack"], ans: 1, exp: "Shared memory IPC is achieved by mapping different virtual addresses from different processes to the same physical memory frames. No copying is required." },
  { id: "os100", diff: "scenario", topic: "CPU Scheduling", q: "Scenario: A system is using Shortest Remaining Time First (SRTF) scheduling. Process P1 (burst 10ms) arrives at 0ms. P2 (burst 2ms) arrives at 2ms. What happens at 2ms?", opts: ["P1 continues until 10ms", "P1 is preempted. P2 executes for 2ms because its remaining burst (2ms) is shorter than P1's remaining burst (8ms). After P2 finishes, P1 resumes", "Both execute simultaneously", "The system crashes"], ans: 1, exp: "SRTF is the preemptive version of SJF. When a new process arrives with a shorter burst than what the current process has remaining, the current process is immediately preempted." },
  { id: "os101", diff: "easy", topic: "Process Management", q: "Which component is responsible for translating a virtual address to a physical address?", opts: ["CPU ALU", "Memory Management Unit (MMU)", "Hard Drive Controller", "The Scheduler"], ans: 1, exp: "The MMU is a hardware component that performs the mathematical translation (via Page Tables and TLB) from logical/virtual addresses to physical RAM addresses." },
  { id: "os102", diff: "medium", topic: "Memory Management", q: "What is Segmentation?", opts: ["Dividing memory into equal-sized pages", "Dividing memory into variable-sized logical segments that reflect the user's view of a program (e.g., Code, Data, Stack)", "Splitting the CPU into cores", "Encrypting memory blocks"], ans: 1, exp: "While Paging divides memory into fixed physical blocks (pages/frames), Segmentation divides it into variable logical units. They are often combined (Segmented Paging)." },
  { id: "os103", diff: "medium", topic: "Security & Protection", q: "What is the purpose of an OS 'Trap' or 'Exception'?", opts: ["To catch viruses", "A software-generated interrupt caused either by an error (e.g., divide by zero) or a specific request from a user program (system call)", "To speed up the network", "To halt the computer permanently"], ans: 1, exp: "A trap switches the CPU into kernel mode and jumps to a predefined trap handler in the OS, ensuring the user program cannot arbitrarily execute kernel code." },
  { id: "os104", diff: "hard", topic: "Deadlocks", q: "If a system has 3 processes and each requires 2 units of a resource R. What is the minimum total units of R needed to guarantee that Deadlock will NEVER occur?", opts: ["6", "5", "4", "3"], ans: 2, exp: "Formula: If N processes each need Max M resources, Deadlock is impossible if Total R >= N * (M - 1) + 1. Here: 3 * (2 - 1) + 1 = 4. With 4 resources, at least one process will get its full 2 resources." },
  { id: "os105", diff: "scenario", topic: "Threads & Concurrency", q: "Scenario: A multi-threaded web server creates a new thread for every incoming HTTP request. During a massive traffic spike, the server crashes due to 'Out of Memory'. What is the optimal OS architecture fix?", opts: ["Increase RAM infinitely", "Switch from thread-per-request to a Thread Pool, limiting the maximum number of concurrent threads and queuing excess requests", "Use multi-processing instead of multi-threading", "Disable all networking"], ans: 1, exp: "A Thread Pool bounds the number of threads. Threads consume memory (their stack) and cause heavy context-switching overhead. A pool reuses a fixed number of threads." }
];
