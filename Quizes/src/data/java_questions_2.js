// Java Questions — Batch 2: Collections Framework (40 questions)
export const JAVA_Q2 = [
  // ═══ COLLECTIONS — LIST (10) ═══
  { id:"java41", diff:"easy", topic:"Collections", q:"What is the difference between ArrayList and LinkedList?", opts:["No difference","ArrayList: dynamic array, O(1) random access, O(n) insert/delete in middle. LinkedList: doubly linked list, O(n) access, O(1) insert/delete at known position","LinkedList is faster for everything","ArrayList uses linked nodes"], ans:1, exp:"ArrayList: backed by Object[]. Fast get(i) O(1), slow add/remove in middle O(n) due to shifting. LinkedList: fast add/remove at head/tail O(1), slow get(i) O(n) — must traverse." },
  { id:"java42", diff:"easy", topic:"Collections", q:"What interface do ArrayList and LinkedList both implement?", opts:["Set","Map","List","Queue only"], ans:2, exp:"Both implement List<E> (ordered, allows duplicates, index-based access). LinkedList also implements Deque<E>. List extends Collection extends Iterable." },
  { id:"java43", diff:"medium", topic:"Collections", q:"How does ArrayList grow when capacity is exceeded?", opts:["Doubles size","Grows by 50% (newCapacity = oldCapacity + oldCapacity >> 1), copies elements to new array","Grows by 1 element","Throws exception"], ans:1, exp:"ArrayList default capacity: 10. When full, creates new array ~1.5× size and copies elements (System.arraycopy). Amortized O(1) for add. Pre-size with new ArrayList<>(expectedSize) to avoid resizing." },
  { id:"java44", diff:"medium", topic:"Collections", q:"What happens when you modify a List during iteration with for-each?", opts:["Nothing","ConcurrentModificationException — the iterator detects structural modification via modCount check","Elements are skipped","Null values appear"], ans:1, exp:"ArrayList's iterator checks modCount. If the list is structurally modified (add/remove) outside the iterator, it throws ConcurrentModificationException. Use Iterator.remove() or CopyOnWriteArrayList instead." },
  { id:"java45", diff:"medium", topic:"Collections", q:"What is the difference between Iterator and ListIterator?", opts:["Same thing","Iterator: forward only, remove(). ListIterator: bidirectional (next/previous), add(), set(), index-based — only for Lists","ListIterator is faster","Iterator supports add()"], ans:1, exp:"ListIterator extends Iterator. Extra: hasPrevious(), previous(), nextIndex(), previousIndex(), add(), set(). Only available for List implementations, not Set or Queue." },

  // ═══ COLLECTIONS — SET (8) ═══
  { id:"java46", diff:"easy", topic:"Collections", q:"What is the main property of a Set?", opts:["Ordered elements","No duplicate elements — uses equals() and hashCode() to determine uniqueness","Sorted elements","Index-based access"], ans:1, exp:"Set stores unique elements. Adding a duplicate returns false. HashSet uses hashCode()+equals(). TreeSet uses compareTo()/Comparator. LinkedHashSet maintains insertion order." },
  { id:"java47", diff:"medium", topic:"Collections", q:"What is the difference between HashSet, LinkedHashSet, and TreeSet?", opts:["No difference","HashSet: unordered, O(1). LinkedHashSet: insertion order, O(1). TreeSet: sorted (natural/Comparator), O(log n)","All are sorted","All maintain insertion order"], ans:1, exp:"HashSet: HashMap-backed, no order guarantee, O(1) add/remove/contains. LinkedHashSet: doubly linked list maintains insertion order. TreeSet: Red-Black tree, sorted, O(log n) operations." },
  { id:"java48", diff:"medium", topic:"Collections", q:"Can HashSet contain null?", opts:["No","Yes — exactly one null element is allowed","Multiple nulls allowed","Throws NullPointerException"], ans:1, exp:"HashSet allows one null (hashed to bucket 0). TreeSet does NOT allow null (compareTo() throws NPE). LinkedHashSet allows one null. HashMap allows one null key, multiple null values." },
  { id:"java49", diff:"hard", topic:"Collections", q:"How does HashSet internally prevent duplicates?", opts:["Compares all elements","Uses HashMap internally — element is the key, a dummy PRESENT object is the value. HashMap handles uniqueness via hashCode()+equals()","Sorts and removes duplicates","Uses a Trie"], ans:1, exp:"HashSet = HashMap<E, Object> where all values are the same PRESENT sentinel. add(e) calls map.put(e, PRESENT). If key exists, put() returns old value (non-null) → add returns false." },

  // ═══ COLLECTIONS — MAP (12) ═══
  { id:"java50", diff:"easy", topic:"Collections", q:"What is a HashMap?", opts:["A sorted map","A key-value store with O(1) average get/put, backed by an array of buckets using hash-based indexing","A thread-safe map","A LinkedList of entries"], ans:1, exp:"HashMap: hash(key) → bucket index. Stores Entry<K,V> objects. O(1) average for get/put/remove. Does not guarantee order. Allows one null key, multiple null values." },
  { id:"java51", diff:"medium", topic:"Collections", q:"What is the internal structure of HashMap in Java 8+?", opts:["Simple array","Array of buckets. Each bucket is a linked list. When a bucket has ≥8 entries (and capacity ≥64), it converts to a Red-Black Tree for O(log n) worst case","B-Tree","Hash table with open addressing"], ans:1, exp:"Java 8: array of Node<K,V>. Collision → linked list (O(n) worst case). When chain ≥ TREEIFY_THRESHOLD (8) and capacity ≥ 64, bucket treeifies → Red-Black Tree (O(log n)). Untreeifies at ≤6." },
  { id:"java52", diff:"medium", topic:"Collections", q:"What is the default initial capacity and load factor of HashMap?", opts:["10 and 0.5","16 and 0.75","32 and 0.8","8 and 0.5"], ans:1, exp:"Default capacity: 16 (must be power of 2). Load factor: 0.75 (resize at 75% full). Threshold = capacity × loadFactor = 12. When 13th entry added, capacity doubles to 32." },
  { id:"java53", diff:"medium", topic:"Collections", q:"What happens during HashMap resizing (rehashing)?", opts:["Nothing","Creates a new array of 2× capacity, rehashes ALL existing entries to new bucket positions — O(n) operation","Only moves the last bucket","Throws exception"], ans:1, exp:"Resize: new Node[oldCapacity*2]. Each entry is rehashed (hash & (newCapacity-1)). In Java 8, entries stay at same index OR move to index+oldCapacity (optimization using hash bit). O(n) worst case." },
  { id:"java54", diff:"medium", topic:"Collections", q:"How does HashMap.get(key) work internally?", opts:["Linear scan","1. hash(key) → bucket index. 2. Traverse bucket (list/tree). 3. For each node: if hash matches AND (key == node.key OR key.equals(node.key)) → return value","Binary search on keys","Sorts keys first"], ans:1, exp:"Steps: (1) Compute hash, find bucket (hash & (capacity-1)). (2) Check first node. (3) If collision chain: traverse linked list or search Red-Black Tree. (4) Match by hash THEN equals()." },
  { id:"java55", diff:"medium", topic:"Collections", q:"What is the difference between HashMap, LinkedHashMap, and TreeMap?", opts:["All are same","HashMap: no order, O(1). LinkedHashMap: insertion/access order, O(1). TreeMap: sorted by key (natural/Comparator), O(log n)","All are sorted","All are thread-safe"], ans:1, exp:"HashMap: hash table, unordered. LinkedHashMap: hash table + doubly linked list (preserves insertion or access order — basis for LRU cache). TreeMap: Red-Black tree, NavigableMap, sorted keys." },
  { id:"java56", diff:"hard", topic:"Collections", q:"Why must hashCode() be overridden when equals() is overridden?", opts:["Style convention only","If equals() says two objects are equal but hashCode() differs, HashMap will store them in different buckets — making it impossible to find the 'equal' key","hashCode is never used","Performance only"], ans:1, exp:"HashMap uses hashCode to find the bucket, then equals to find the key within. If equal objects have different hashCodes → different buckets → get() can't find the key even though equals() matches." },
  { id:"java57", diff:"hard", topic:"Collections", q:"What makes a good hashCode() implementation?", opts:["Always return 0","Distribute objects uniformly across buckets. Use prime number multiplication (31 in Java). Include all fields used in equals(). Consistent: same object → same hash","Return object address","Random number"], ans:1, exp:"Bad hashCode (e.g., return 0): all entries in one bucket → O(n). Good hashCode: uniform distribution. Java's String: s[0]*31^(n-1) + s[1]*31^(n-2) + ... + s[n-1]. Objects.hash(fields...) is convenient." },
  { id:"java58", diff:"hard", topic:"Collections", q:"What happens if you use a mutable object as a HashMap key and modify it after insertion?", opts:["Nothing","The key's hashCode changes, so it maps to a different bucket. The entry becomes 'lost' — get() with the same object returns null","Automatic rehash","ConcurrentModificationException"], ans:1, exp:"Modifying a key after insertion changes its hashCode. HashMap doesn't know — the entry stays in the old bucket. get() computes new hash → wrong bucket → null. Use immutable keys (String, Integer)." },
  { id:"java59", diff:"scenario", topic:"Collections", q:"Scenario: You need a Map that maintains insertion order and acts as an LRU cache (evicts oldest accessed). Which class?", opts:["HashMap","LinkedHashMap with accessOrder=true + override removeEldestEntry()","TreeMap","ConcurrentHashMap"], ans:1, exp:"LinkedHashMap(capacity, loadFactor, true): accessOrder=true moves accessed entries to end. Override removeEldestEntry() to evict when size > maxSize. Simple O(1) LRU cache." },
  { id:"java60", diff:"scenario", topic:"Collections", q:"Scenario: HashMap vs Hashtable vs ConcurrentHashMap — which for thread-safe concurrent access?", opts:["HashMap with synchronized block","ConcurrentHashMap — lock-striping (segments/bins), better concurrency than synchronized Hashtable","Hashtable — legacy but works","All are equivalent"], ans:1, exp:"Hashtable: synchronized on entire map (one thread at a time). ConcurrentHashMap: fine-grained locking (Java 8: CAS + synchronized on individual bins). Much better throughput. HashMap is NOT thread-safe." },

  // ═══ COLLECTIONS — QUEUE / PRIORITY QUEUE (5) ═══
  { id:"java61", diff:"easy", topic:"Collections", q:"What is the difference between Queue and Stack in terms of ordering?", opts:["Both are LIFO","Queue: FIFO (First In First Out). Stack: LIFO (Last In First Out)","Queue is LIFO","Stack is FIFO"], ans:1, exp:"Queue: add at tail, remove from head (line at a store). Stack: push/pop from top (stack of plates). In Java, use Deque instead of legacy Stack class." },
  { id:"java62", diff:"medium", topic:"Collections", q:"What is PriorityQueue's internal data structure and ordering?", opts:["Sorted array","Min-heap (binary heap) — elements are dequeued in natural order (or Comparator order). NOT sorted internally","Linked list","Red-Black tree"], ans:1, exp:"PriorityQueue: backed by a resizable array representing a binary min-heap. peek()/poll() return the smallest element. add/offer: O(log n). peek: O(1). NOT thread-safe (use PriorityBlockingQueue)." },
  { id:"java63", diff:"medium", topic:"Collections", q:"What interface should you use for a double-ended queue?", opts:["Queue","Deque — supports add/remove from both ends. ArrayDeque is preferred over LinkedList for Deque usage","Stack","List"], ans:1, exp:"Deque: offerFirst/offerLast, pollFirst/pollLast. ArrayDeque: circular array, no null, faster than LinkedList. Use as stack (push/pop) or queue (offer/poll). Java docs recommend over Stack class." },
  { id:"java64", diff:"scenario", topic:"Collections", q:"Scenario: You need to process tasks by priority (lowest number = highest priority). Which collection?", opts:["ArrayList + sort each time","PriorityQueue<Task> with Comparator.comparingInt(Task::getPriority) — O(log n) insert, O(1) peek","TreeSet","LinkedList"], ans:1, exp:"PriorityQueue: insert O(log n), peek/poll O(1)/O(log n). Automatically maintains heap order. TreeSet also works (O(log n) all operations) but PriorityQueue is more efficient for this pattern." },
  { id:"java65", diff:"scenario", topic:"Collections", q:"Scenario: What is the output?\nPriorityQueue<Integer> pq = new PriorityQueue<>();\npq.add(30); pq.add(10); pq.add(20);\nSystem.out.println(pq);", opts:["[30, 10, 20]","[10, 30, 20] — heap order, not sorted. toString shows internal array, not poll order","[10, 20, 30]","[20, 10, 30]"], ans:1, exp:"PriorityQueue's toString() shows the internal array, which is a heap — NOT fully sorted. Only the root (index 0) is guaranteed to be minimum. Polling one by one gives sorted order: 10, 20, 30." },

  // ═══ COLLECTIONS — ITERATION & GENERAL (5) ═══
  { id:"java66", diff:"medium", topic:"Collections", q:"What is the difference between fail-fast and fail-safe iterators?", opts:["Same thing","Fail-fast (ArrayList, HashMap): throws ConcurrentModificationException on structural modification. Fail-safe (CopyOnWriteArrayList, ConcurrentHashMap): works on snapshot, no exception","Fail-safe throws exceptions","Fail-fast works on copies"], ans:1, exp:"Fail-fast: checks modCount. Any add/remove during iteration → CME. Fail-safe: iterates over a copy (CopyOnWriteArrayList) or uses special lock-free iteration (ConcurrentHashMap). Trade-off: fail-safe may not reflect latest changes." },
  { id:"java67", diff:"medium", topic:"Collections", q:"How do you safely remove elements during iteration?", opts:["list.remove(element) during for-each","Use Iterator.remove() — the only safe way to modify a collection during iteration","Create a new list","Use removeIf() — both B and D work"], ans:3, exp:"Iterator.remove(): safe, works with all collections. Java 8 removeIf(predicate): list.removeIf(x -> x < 0). Both avoid ConcurrentModificationException. Never call list.remove() inside for-each." },
  { id:"java68", diff:"hard", topic:"Collections", q:"What is the time complexity of Collections.sort() and which algorithm does it use?", opts:["Quicksort O(n log n) average","TimSort — hybrid merge sort + insertion sort. O(n log n) worst case, stable, O(n) auxiliary space","Heap sort","Bubble sort"], ans:1, exp:"Java uses TimSort (invented for Python): finds natural runs, merges them. Stable, O(n log n) worst case, O(n) on nearly-sorted data. Arrays.sort() for primitives uses Dual-Pivot Quicksort (unstable, O(n log n) average)." },
  { id:"java69", diff:"scenario", topic:"Collections", q:"Scenario: You need a thread-safe list that supports frequent reads and rare writes. Best choice?", opts:["ArrayList with synchronized block","CopyOnWriteArrayList — copies entire array on write, reads are lock-free O(1)","Vector","LinkedList"], ans:1, exp:"CopyOnWriteArrayList: every write creates a new copy of the underlying array (expensive for writes). But reads are unsynchronized and very fast. Ideal for read-heavy, write-rare scenarios like listener lists." },
  { id:"java70", diff:"scenario", topic:"Collections", q:"Scenario: Which Map implementation would you choose for a cache with max 1000 entries that evicts by least-recently-used?", opts:["HashMap + manual tracking","LinkedHashMap(16, 0.75f, true) with removeEldestEntry returning size()>1000","TreeMap","WeakHashMap"], ans:1, exp:"LinkedHashMap with accessOrder=true reorders on every get(). Override removeEldestEntry(Map.Entry eldest) { return size() > 1000; }. Simple, efficient O(1) LRU cache." }
];
